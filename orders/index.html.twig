{% extends 'base.html.twig' %}

{% block title %}Orders index{% endblock %}



{% block body %}

    
    
    <section class="breadcrumb_section page-title-light background_bg bg_fixed overlay_bg_blue_70" data-img-src="{{asset('deco/assets/images/blue5.jpg')}}"> 
	<div class="container">
    	<div class="row align-items-center">
        	<div class="col-sm-12 text-center">
            	<div class="page-title">
            		<h2><strong><div class="p-3 mb-2 bg-light text-dark">ORDERS INDEX</div></strong></h2>

                    {#<style type="text/css">
@font-face {
    font-family: "Monoton-Regular";
    src: url(deco\assets\fonts\Monoton-Regular.ttf);
}
p.customfont { 
    font-family: "Monoton-Regular" !important;
}
</style>
<p class="customfont">Hello world!</p>#}
                    
                </div>
            </div>
        </div>
    </div>
</section>


     
  

    
<section class="container">
    <table class="table table-hover table-dark">
        <thead>
            <tr>
                <th>Id</th>
                <th>Utilisateurs</th>
                <th>Produits</th>
                <th>Prix</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for order in orders %}
            <tr>
                <td>{{ order.id }}</td>
                <td>{{ order.user.firstname}}</td>
                <td >
                    <table>
                    {% for item in order.product %}
                     <tr>
                     <td>{{ item.name }} </td> 
                     </tr>
                    {% endfor %}
                    </table>
                </td>
                <td>
                    <table>
                    {% for item in order.product %}
                    <tr>
                     <td class="row"> {{ item.price }} </td> 
                    </tr>
                     {% endfor %}
                    </table>
                </td>
                <td>{{ order.createdAt ? order.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('categories_show', {'id': order.id}) }}">  <div class="p-3 mb-2 bg-light text-dark"><center><strong>Show</strong></center></div></a>
                    <a href="{{ path('categories_show', {'id': order.id}) }}">  <div class="p-3 mb-2 bg-light text-dark"><center><strong>Edit</strong></center></div></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('orders_new') }}"> <div class="p-3 mb-2 bg-light text-dark"><center><strong>Créer Nouveau</strong></center></div></a>
</section>
{% endblock %}
