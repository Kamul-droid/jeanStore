<form method="post" action="{{ path('products_delete', {'id': product.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product.id) }}">
    <center><button class="btn"><div class="p-3 mb-2 bg-dark text-light"><center><strong> Supprimer <br></strong></center></div></a></button></center>
</form>
