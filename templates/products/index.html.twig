{% extends 'base.html.twig' %}

{% block title %}Products index{% endblock %}

{% block body %}
 <section class="breadcrumb_section page-title-light background_bg bg_fixed overlay_bg_blue_70" data-img-src="{{asset('deco/assets/images/blue5.png')}}"> 
	<div class="container">
    	<div class="row align-items-center">
        	<div class="col-sm-12 text-center">
            	<div class="page-title">
            		<h2><strong><div class="p-3 mb-2 bg-light text-dark">{% trans %}PRODUITS{% endtrans %}</div></strong></h2>

                    
                </div>
            </div>
        </div>
    </div>
</section>
<section class="container">
    <table class="table table-hover table-dark">
        <thead>
            <tr>
                <th>{% trans %}Id{% endtrans %}</th>
                <th>{% trans %}Utilisateurs{% endtrans %}</th>
                <th>{% trans %}Produits{% endtrans %}</th>
                <th>{% trans %}Prix{% endtrans %}</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for product in products %}
            <tr>
               <td>{{ product.id }}</td>
                <td>{{ product.price }}</td>
                <td>{{ product.name }}</td>
                <td>{{ product.categorie.name }}</td>
                <td>{{ product.quantity }}</td>
                <td>{{ product.createdAt ? product.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('products_show', {'id': product.id}) }}"><div class="p-3 mb-2 bg-light text-dark"><center><strong>{% trans %}Voir{% endtrans %}</strong></center></div></a>
                    <a href="{{ path('products_edit', {'id': product.id}) }}"><div class="p-3 mb-2 bg-light text-dark"><center><strong>{% trans %}Modifier{% endtrans %}</strong></center></div></a>
                </td>
            </tr>
        {% else %}
           <tr>
                <td colspan="7">{% trans %}no records found{% endtrans %}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('products_new') }}"> <div class="p-3 mb-2 bg-light text-dark"><center><strong>{% trans %}Créer Nouveau{% endtrans %}</strong></center></div></a>


</section>
{% endblock %}

